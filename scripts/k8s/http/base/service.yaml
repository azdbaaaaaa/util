apiVersion: v1
kind: Service
metadata:
  name: $SERVICE
  annotations:
  labels:
    app: $SERVICE
spec:
  ports:
    - name: grpc
      protocol: TCP
      port: 8088
      targetPort: $PORT
  selector:
    app: $SERVICE
